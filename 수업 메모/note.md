
## 260506

##  1. 도구 (Tool)
**정의:** 직접 사용해서 무형의 가치를 만드는 장비
- **옵시디언(Obsidian):** 지식 관리 및 기록 도구 (에디터) -md전용
- **커서(Cursor):** 프로그램 편집기 (에디터).
- **크롬(Chrome):** 웹 브라우저 및 정보 검색 도구.
- **깃(Git):** 코드 버전 관리 도구.
- **pip:** 라이브러리(도구들의 묶음) 관리자.
- **venv:** 프로젝트별 독립된 개발 환경.

##  2. 저장소 (Repository)
**정의:** 코드와 기록이 흩어지지 않게 보관하는 보관함
- **깃허브(GitHub):** 온라인상의 코드 저장소 및 협업 공간.
- **로컬 레포지토리:** 내 컴퓨터(PC) 내부에 존재하는 물리적인 코드 저장소.

##  3. 플랫폼 (Platform) vs 프로그램 (Program)
**정의:** 활동할 수 있도록 판을 깔아주는 곳 (변경 가능 여부에 따른 구분)
- **플랫폼:** 우리가 활동할 수 있는 기반. 사용자에 의해 **변경 가능**함. (예: 가비아, 캐글, 클라우드 서버)
- **프로그램:** 특정 목적을 위해 실행되는 소프트웨어. 일반적으로 사용자가 구조를 **변경 불가능**함.

##  4. 방법 (Method) 및 기록
- **방법(Method):** 문제를 해결하는 논리적인 절차나 레시피. (예: 알고리즘, 이진 분류, 정렬)
- **아카이브(Archive):** 가치 있는 기록을 장기 보관하기 위해 체계적으로 정리한 창고.

**상속 관계:** `클래스(Class)` > `메소드(method)` > `알고리즘(algorithm)
`
- **생성자:** 객체(Object)를 실제로 만들어내는 특수한 메소드.
- **디폴트(Default):** 아무것도 하지 않아도 주어지는 기본값. (당연한 설정에는 굳이 명시하지 않음)
- **RAM:** 메인 메모리 + 서브 메모리 체계.
- **md(markdown)** - 컴퓨터가 정확히 해석할 수 있도록 <>꺽쇠를 앞뒤로 붙이는 '태그'  
< - > **markup** - 사람이 읽고 쓰기 편하게 최소한의 기호만 사용
**mark** - 

##  5. 브리즈 (Brackets - 괄호 명칭)
| 모양 | 명칭 (한글) | 명칭 (영문) |
| :--- | :--- | :--- |
| **( )** | 라운드 | Round Brackets |
| **{ }** | 컬 | Curly Brackets |
| **[ ]** | 스퀘어 | Square Brackets |
| **< >** | 다이아몬드 | Angle Brackets |


-------------------------------------
## 260507

AI---------------
    생성형
    에이전틱
  ---------------
  
상수(콘스탄트) - 항상 똑같은 것
변수(배러블) - 변하는 것

**A -> B (A는 B이다)** 일때 부모는 항상 B ( -> ) 화살표는 is를 의미


**라이브러리** = 휴먼'이' 필요할 때 사용
**프레임워크** = 휴먼'을' 필요할 때 사용 (=FastAPI)

## DI (Dependency Injection) -> 의존성 주입
휴먼이 사용하던 라이브러리를 시스템 자동화를 통해 프레임워크(머신)에게 주입하는 것

API => 프론트엔드 <-> 백엔드 서로 정보를 주고받는 것


-------------------------------------
## 260508

알고리즘 <- **대수학([代](https://namu.wiki/w/%E4%BB%A3 "代")[數](https://namu.wiki/w/%E6%95%B8 "數")[學](https://namu.wiki/w/%E5%AD%B8 "學"), algebra)** - 수를 대체하여 문자로 정리한 것

## 상수와 변수
**콘스탄트(constant)** - 변하지않는 값, 대문자
**리터럴(Literal)** - 고정된 값, 소문자 => 구조를 만든다
**배러블(Variable)** - 변수

**객체(Object)**
ㄴ> ( 인스턴스 <-> 클래스 )
ㄴ 메모리에 있으면 인스턴스 디스크에 있으면 클래스

디스크(클래스)에서 메모리(인스턴스)로 보내는 것 => 로드
메모리(인스턴스)에서 디스크(클래스)로 보내는 것 => 세이브


> data는 변하지 않고 value는 변한다

[클래스]**data** - 대문자, 변하지 않음
[인스턴스]**값(Value)** - pandas가 data를 읽으면 value로 바뀐다, 소문자 (= 변수값)
 ㄴ AI전용
 
 디스크에서 클래스 = 메모리에서 인스턴스 
ㄴ 필드와 메서드로 나뉨

null - 비어있음

-----------------------
## 260511

repository - 저장소

### 연관(Association) 관계 <-> 의존(Dependency) 관계
**연관관계** : 지속적인 관계 (사람과 심장) - 없으면 죽음
**의존관계** : 일시적인 관계 - 일시적으로 사용 가능

런타임(프로세스) - 움직이는중
프로그램 - 움직임이 없는것
스레드는 메서드로 통제

## 필드와 에어리어
![[필드와에어리어.png]]
필드 = 속성
에어리어 = 기능 (에어리어에 해당하지 않는 부분은 다 속성) = 메소드
**객체는 속성과 기능의 집합**

----------------
## 260512

# 디자인 패턴 
![[Pasted image 20260512100334.png]]
구조 객체->개체 7개
행위 개체->함수 11개
생성 함수-> 개체 5개

	프로세스 - 메모리
	
	프로젝트 - 디스크
	
	프로그램 - 메모리, 프로세스의 집합
	(프로그래밍 - 프로그램을 만드는 과정)
	
	프로덕트 - 디스크, 제품 
	
**MVC(Model-View-Controller)**
M -> 객체
V -> 함수
C -> 함수
[모델]을 만들고 [뷰]를 만들어서 [컨트롤러]로 연결한다
ㄴ백엔드          ㄴ프론트엔드

**~형 = type**

	- 객체(파이썬) -> object(오브젝트) = 기능(+)속성 ---> 변화, 저장 o
	- 개체(SQL) -> entity(엔티티) = 객체(-)기능 ---> 저장만 o
	- 함수(HTML) -> F(x)(펑션) 객체(-)속성 ---> 변화만 o

## 연결
obj - f(x)  = RESTful API
obj - ntt = connection (비공개-보안)

obj - ntt - f(x) = web
각각의 프레임워크를 갖게됨

함수형 프로그래밍은 객체 지향 프로그래밍(OOP)의 반대

--------------------------

## 260513

### 데이터베이스
	관계형(RDBMS)
	비관계형(NoSQL)

### 연동
동기(sync) - (전화) 요청하고 대답이 돌아올때까지 대기, 다른작업X
비동기(async) - (카톡) 요청하고 대답의 유무와 상관없이 작업 수행, 다른작업O 
  ㄴ비동기를 주로 사용

-----------------------------

## 260514

**객체지향 프로그래밍(Object Oriented Programming)**

패턴 - 추상
아키텍처 - 구상

모듈 방식 아키텍처
	클린


아키텍처
	모놀리식(Monolithic Architecture) - 단일코드베이스
	  ㄴ 모노레포
	  ㄴ 서브모듈
	MSA(Microservices Architecture) - 확장 가능

-----------------------------

## 260515

string = 문자열
int = 정수형

프렉탈구조 - 같은게 계속 반복되다가 최후에 기능+속성 만 남음

   o-------------------o
axios                   rest 

리퀘스트 : axios --> rest
리스폰스 : axios <-- rest

하이퍼텍스트
	하이퍼링크 - 링크된 텍스트
	하이퍼텍스트 - 하이퍼링크가 들어가있는 텍스트

페이지 이동 방식
	하이퍼링크
	폼 태그

----------------------------------

## 260518

**spiral(스파이럴)** - 나선형 모델, 4단계를 반복

**backend 구조**
ㄴ get 방식, post 방식
	대화방식의 차이(질문의 유무)
	get - 질문 X, 고정
	post - 직접 질문
	
![[Pasted image 20260518120721.png]]
함수는 Html과 JavaScript로 나뉜다

프레임워크 -> 리파인 가능 / Refine (리파인) - 코드를 내 맘대로 고칠수O
	Declaration(디클러레이션) - 선언문 : 변화X
	Define(디파인) - 정의 => 데피니션(Definition) : 변화O
	Fine-tuning (파인튜닝) - 원래있던것을 바꿈
* 디클러레이션 > 디파인
![[143603.png]]
**컨<서<레<엔**
컨 : 컨트롤러
서 : 서비스
레 : 레포지토리
엔 : 엔티티

화면 > 메인 > 컨트롤러 > 서비스 > 레포지토리 > 엔티티


---------------------------------------

## 260519

서브모듈 만드는 이유 : 모듈러방식
ㄴ 사용하는 이유 : MSA의 장점과 모놀리식의 장점을 다 가지고가기위해

form 태그 - 상태를 만드는 태그

**RESTful API** : 상태를 전송한다
상태
ㄴ현재상태
ㄴ무상태(stateless)
![[stateless.png]]
.                request
클라이언트  ㅡㅡㅡㅡ> 서버
클라이언트 <ㅡㅡㅡㅡ 서버
.                response

**프로토콜(HTTP)**
상태변화X (Query)
get : DB에 있는 정보 알려줘 => read
**ㅡㅡㅡDB 상태변화의 유무ㅡㅡㅡ**
상태변화O (command)
post : DB에 저장해 => create 
put : DB에 있는 데이터 수정해 =>update
delete : DB에서 삭제해
 ㄴ> (자연어로)앞글자만 따서 **crud** 라고 부름!

context : 맥락
컨텍스트 > 클래스 > 메소드(상수) > 변수
상수 = 함수
변수 = 상수안에있는영역
#### alembic
![[a.png]]

-----------------------------------------
## 260520

"" <- 스트링

notation : 기호분류하기
ex) 피곤해요>?< / 피곤해요>!<  >>물음표와 느낌표의 차이
annotation : 주석


const 
ㄴ 상수 (불변의 값)
ㄴ 기능 ("=>" 화살표 붙어있을때)

@ => 데코레이터 패턴
@가 붙어있으면 함수 밖에도 영향을 주고 붙어있지 않으면 함수 내에서만 영향
데코레이터 패턴을 사용하면 기능이 바뀜!
ex) 생크림케이크에 초코바르면 초코케이크


---------------------
## 260521

attribute - static / 정적 / 상태는 변하지 않는다(상수)
property - dynamic / 동적 / 상태는 변한다(변수)

**객체 지향 프로그래밍(OOP)의 4대 특징**
* 은닉화(=캡슐화)(Encapsulation)
* 상속(Inheritance)
* 추상화(Abstraction)
* 다형성(Polymorphism)
=> 객체(OOP) 은 상추다

{ } => JSON
JSON과 DOM은 객체가 아니었는데 은상추다에 의해 객체가 됨

Database : 
Dataset : 파일형태

**ERD** : Entity Relationship Diagram 개체 관계 다이어그램 => 모델링
ㄴ 개체(Entity)를 객체(Object)로 만드는 설계도


----------------------------------------------

## 260522

정규화(Normalization) : 중복을 없애고 데이터가 쪼개져있는 상태(-> 테이블을 분해)
(= 데이터가 중복없이 테이블로 잘 분해되어있는 상태)

![[Pasted image 20260522130439.png]]
1NF : 모든 도메인이 원자값(Atomic Value)을 가져야 함(하나의 값만 들어가야 함)

제1정규형 (1NF) -> 중복이 있으면 안됨

	식별(실선) : 부모X = 나X
	비식별(점선) : 부모X but 나O
	
	- IS-A 관계 (-은 -이다) (=life cycle) - 식별
	- HAS-A 관계 (-은 -을 가진다) - 비식별
	- USE-A 관계 (-은 -을 사용한다) - 비식별

제2정규형 (2NF)

	- 함수적 종속성(Functional Dependency)
제3정규형 (3NF)

	- 다 대 다 관계는 교차엔티티로 해소

-------------------------

## 260526

**CQRS패턴** - DB의 상태변화 유무로 command(변하면)와 Query(변하지않으면)로 나뉜다

----------------------
## 260527

**SQL**
	(Query)
	DQL - 테이블을 읽음
	-----------------------
	(command)
	DML - 테이블을 CRUD 함
	DDL - 테이블을 만들어줌
	DCL - DB 권한을 줌
![[SQL.png]]
어댑터(adapter)패턴 (입구와 출구 분리)
![[단방향.png]]

추상화(python) ->  인터페이스 
= 구현(DB) -> impl (implementation)
= 실체값은 없는 상태로 맥락만 잡음

-----------------------
## 260528

**클린 아키텍처(SOLID)**
	SRP : 단일 책임 원칙
	OCP : 개방-폐쇄 원칙
	LSP : 리스코프 치환 원칙
	ISP : 인터페이스 분리 원칙(= 추상화)
	DIP : 의존성 역전 원칙

인바운드 = 라우터의 집

화면 -> 인바운드 -> 인풋 -> 유스케이스 -> 아웃풋 -> 아웃바운드
