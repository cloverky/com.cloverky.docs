기계어를 자연어로 바꾼것(기계어도 자연어도 아님)

-venv 드가는 명령어-
conda activate venv

-기존에 캐시를 지우는 명령어- 
taskkill /F /IM python.exe

-의존성 주입하는 명령어-
pip install -r requirements.txt

-새로 시작하는 명령어-

-환경변수 바로 드가는 명령어-
윈도우 r누르고
sysdm.cpl ,3

* import A as B - A를 B로 부른다
* print(df.head(n)) - n줄 출력
* init - 초기화 시킴

저장해야하는 데이터 실선으로 표시 (연관관계)
일회용으로 사용하는 데이터 점선으로 표시 (의존관)

=> : 화살표함수, 애로우펑션(=핸들러) :메소드 역할

지역변수/전역변수
![[변수.png]]

