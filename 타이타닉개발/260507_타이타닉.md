# Titanic API Project Specification
#127.0.0.1:8000/data

이 파일은 Cursor AI가 프로젝트의 초기 상태와 구조를 학습하기 위한 가이드라인입니다.[cite: 1]

## 1. 프로젝트 기본 정보
- **Framework**: FastAPI[cite: 1]
- **Project Name**: Titanic API[cite: 1]
- **Key Phrase**: "제임스가 메인이다"[cite: 1]

## 2. 코드 구조 (main.py)
```python
from fastapi import FastAPI, Query
try:
    from .walter import Walter
except ImportError:
    from walter import Walter

app = FastAPI(title="Titanic API")

class james:
    def __init__(self):
        pass

@app.get("/")
def read_root():
    return {"message": "제임스가 메인이다"}

@app.get("/data")
def get_data(limit: int = Query(default=10, ge=1, le=100)):
    w = Walter()
    return {"rows": w.get_data(limit)}

if __name__ == "__main__":
    import uvicorn
    print("제임스가 메인이다.")
    w = Walter()
    w.get_data()
```[cite: 1]

## 3. Cursor 학습 가이드
1. **Context 설정**: 이 `.md` 파일을 프로젝트 루트에 저장하고 Cursor에서 `@filename`으로 참조하십시오.[cite: 1]
2. **개발 원칙**:
    - 새로운 엔드포인트 추가 시 `Walter` 클래스의 메서드를 활용할 것.[cite: 1]
    - 서버 실행 시 "제임스가 메인이다." 문구가 출력되는 구조를 유지할 것.[cite: 1]
    - API 쿼리 파라미터는 `fastapi.Query`를 사용하여 유효성 검사를 수행할 것.[cite: 1]

## 4. 모듈 설명
- `Walter`: 실제 데이터 로직 및 DB 연동을 담당하는 클래스 (walter.py).[cite: 1]
- `james`: 현재 초기화 상태를 상징하는 클래스.[cite: 1]